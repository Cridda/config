# DO NOT CHANGE!
spring.application.name=account-ms
server.port=8080

# CAN CHANGE
spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=jdbc:postgresql://tribank.trifork.nl:5432/account-ms
spring.datasource.username=production
spring.datasource.password=SecurePassword132@
flyway.enabled=false
management.security.enabled=false
message =  What would the world be like, if we didn't have Spring?

#logging
logging.level.org.springframework.web=INFO
logging.level.com.mkyong=INFO
#output to a file
logging.file=application.log
# Logging pattern for the console
#logging.pattern.console= "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
# Logging pattern for file
logging.pattern.file= "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"